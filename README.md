⚡ VoltDeal Hub — Frontend
🛠️ Tecnologias
TypeScript

Vite

Capacitor (para transformar em app mobile: iOS/Android)

HTML / CSS

🧱 Estrutura do Projeto
📁 Diretórios e Arquivos

**src/ Pasta principal do código-fonte. Aqui devem estar os componentes, páginas e lógica da aplicação.**

**index.html Arquivo base HTML usado pelo Vite para montar a aplicação. É o ponto de entrada do frontend.**

**package.json Define as dependências, scripts e metadados do projeto. Aqui você encontra comandos como npm run dev.**

**package-lock.json Garante que as versões das dependências sejam consistentes em todas as máquinas.**

**vite.config.ts Arquivo de configuração do Vite, usado para ajustar comportamento de build, plugins, etc.**

**.gitignore Lista de arquivos e pastas que o Git deve ignorar (como node_modules, arquivos temporários, etc).**

**README.md Documento de apresentação do projeto.**

🚀 Como rodar o projeto
No diretório raiz, instale as dependências:

bash
**npm install**
Inicie o servidor de desenvolvimento:

bash
**npm run dev**

📱 Mobile
Se necessário, a versão mobile será gerada com Capacitor.
